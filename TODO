1、协程调度模块
             1-N         1-M
  scheduler ---->thread ----> fiber
  1)线程池，分配一组线程
  2)协程调度器，将协程，指定到相应的线程上
2、IO协程调度模块

3. IOManager(Epoll) --> Scheduler
        |
        |
        V
      idle(epoll_wait)
4. Timer --> addTimer() --> cancel()
    Get the time difference between the current timer and now.
    return which need trigger timer

5. Add a folder which storage scripts that can set environment and manager submodule.
